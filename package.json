{
  "name": "rps_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "first-run": "npm install && (cd back && npm install) && (cd front && npm install) && npm start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd back && node server.js\" \"cd front && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.1",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "reactstrap": "^8.0.1"
  }
}
